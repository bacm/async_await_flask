
services:
  flask-wsgi:
    build: .
    container_name: flask-wsgi
    ports:
      - "5001:5000"
    environment:
      - FLASK_ENV=production
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
