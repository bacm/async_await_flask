
services:
  flask-async-trap:
    build: .
    container_name: flask-async-trap
    ports:
      - "5002:5000"
    environment:
      - FLASK_ENV=production
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
